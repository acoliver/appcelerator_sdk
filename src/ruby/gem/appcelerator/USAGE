Description:
    The 'appcelerator' command creates a new Appcelerator on Rails application with a default
    directory structure and configuration at the path you specify.

Example:
    appcelerator ~/Code/Ruby/weblog

    This generates a skeletal Appcelerator installation in ~/Code/Ruby/weblog.
    See the README in the newly created application to get going.

