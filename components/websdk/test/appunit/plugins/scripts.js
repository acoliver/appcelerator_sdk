
test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');

test.assert(typeof($.fn.script)=='function');




